import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'EJE03-GMLYEOB',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
